// Production: replace this with the URL printed by `wrangler deploy`.
// Local testing on localhost automatically uses the local Worker.
window.GG_STANDALONE_QUIZ = {
  apiUrl: 'https://guestguard-inspector-quiz-api.guestguard.workers.dev',
  localApiUrl: 'http://localhost:8787',
  seed: 'demo-even-001',
  passingScore: 80
};
