{
  "name": "guestguard-inspector-quiz-api",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "db:local": "wrangler d1 execute inspector-quiz-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute inspector-quiz-db --remote --file=./schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
